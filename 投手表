# 📋 项目交接文档 — 广告投手提成管理系统

> 本文档用于新对话窗口衔接，请将全文复制粘贴给新的 Claude 对话。
> 整理时间：2026-06-15（v7 文件 + Google Sheets 线上修复版）

---

## 一、项目概览

### 交付物清单

| 文件名 / 位置 | 说明 | 状态 |
|---|---|---|
| `投手提成_10投手版_v7.xlsx` | 本地主文件，13 个 sheet | ✅ 结构已定稿 |
| Google Sheets（线上版） | 实际使用中，已有真实数据 | ⚠️ A23 渠道汇总公式待修复（见七） |
| 账户列表 sheet | 统一维护账户信息 | ✅ 已有 80+ 条真实数据 |

### 业务背景

管理多名「投手」（广告投放人员）的每日广告投放数据与提成结算。

```
当日提成 = 提成比例 × (有效获客数 × 单价 − 实际总消耗 − 当日开户成本合计)
```

---

## 二、当前文件规格（v7）

### 2.1 文件结构（13 个 sheet）

```
汇总 → 账户列表 → 投手1 → 投手2 → ... → 投手10 → 使用说明
```

> ⚠️ **重要**：Google Sheets 线上版中，投手1~10 的**显示标题（A1）**已被改为真人名字（豆腐/旺仔/薯条/u桑/杰克/rich/投手7~10），但 **sheet 标签名本身仍是「投手1」~「投手10」**，没有改。这是正确的做法——汇总公式引用的是 sheet 标签名，不能改标签名，只能改 A1 显示标题。

### 2.2 账户列表 sheet

| 列 | 名称 | 说明 |
|---|---|---|
| A | 账户编号 | 唯一标识，手填 |
| B | 账户简称 | 手填 |
| C | 渠道 | 手填 |
| D | 日期 | 手填，记录登记日期（线上版新增，xlsx本地版无此列，不影响功能） |

- 由**一个人统一维护**，新开账户先在此登记
- 投手 sheet 通过 VLOOKUP 自动读取简称（C列）和渠道（B列）

### 2.3 投手 sheet 规格（投手1~10，结构完全一致）

**列结构（A~T，20 列）**

| 列 | 名称 | 类型 |
|---|---|---|
| A | 日期 | 手填 |
| B | 账户渠道 | 公式：`VLOOKUP(D, 账户列表, 3)` |
| C | 账户简称 | 公式：`VLOOKUP(D, 账户列表, 2)` |
| D | 账户编号 | **手填，唯一人工输入项** |
| E | 账户状态 | 下拉：正常/封控 |
| F | 当日充值 | 手填 |
| G | 广告消耗 | 手填 |
| H | 手续费 | 手填 |
| I | 实际总消耗 | 公式：G+H |
| J~M | 账户/个号/BM/IP成本 | 手填，仅首次开户当天填 |
| N | 成本合计 | 公式：J+K+L+M |
| O | 累计充值 | SUMIFS 按 C列(账户简称)+日期 |
| P | 累计消耗 | SUMIFS 只算广告消耗（G列） |
| Q | 账户余额 | O−P |
| R | 单价 | 手填 |
| S | 有效获客数 | 手填 |
| T | 当日提成 | 公式：`$A$4×(S×R−I−N)` |

**数据区**：第 10~5900 行（5891 行）
**合计行**：第 5901 行（F/G/H/I/J/K/L/M/N/S/T 列有 SUM 公式，O/P/Q 留空）
**冻结**：F10（前 9 行 + 前 5 列）
**自动筛选**：A9:T5900
**数据验证**：仅 E10:E5900（正常/封控下拉）

### 2.4 仪表盘（每个投手 sheet，Row 3~6，8 项）

| Row | 内容 |
|---|---|
| 3/4 | 提成比例（A4，浅黄底，默认 30%）\| 当前正常账户数（F4）\| 当前封控账户数（K4）\| 存活账户余额（P4） |
| 5/6 | 本期累计总充值（A6）\| 本期累计开户成本（F6）\| 本期累计消耗（K6）\| 本期累计提成-已扣成本（P6） |

**数字格式**：F4/K4 = `#,##0`，P4 = `¥#,##0.00`，Row6 均为 `¥#,##0.00`
**行高**：Row7 = 7.5（细分隔行）

### 2.5 汇总 sheet 结构（行号已实测）

```
Row 1      主标题
Row 2      时间范围 B2/D2（浅黄底，手填，格式 yyyy-mm-dd，全表联动）
Row 3      空行
Row 4~5    仪表盘 8 项（总充值/总消耗/总提成/存活余额/开户成本/总获客/正常账户数/封控账户数）
Row 6      空行
Row 7      — 按投手汇总 — 标题
Row 8      列标题（灰底）：投手|充值|广告消耗|总实际消耗|开户成本|获客数|提成
Row 9~18   投手1~10 各行（SUMPRODUCT，按日期范围统计）
Row 19     合计行 SUM(B9:B18)
Row 20     空行
Row 21     — 按渠道汇总 — 标题
Row 22     列标题：渠道|充值|广告消耗|总正常账户余额
Row 23~42  渠道数据（20行，A23=UNIQUE公式，B~D=各行引用自己A列）
Row 43     空行
Row 44     — 每日明细 — 标题
Row 45     列标题：日期|渠道|充值|广告消耗
Row 46     QUERY 公式（每日明细数据）
```

---

## 三、核心公式说明

### 3.1 提成公式（T 列）
```
=IF(OR(C10="",AND(S10="",R10="",I10="",N10="")),"",
  $A$4*(N(S10)*N(R10)-N(I10)-N(N10)))
```

### 3.2 账户渠道（B 列）
```
=IF(D10="","",IFERROR(VLOOKUP(D10,账户列表!$A:$C,3,0),"未登记"))
```

### 3.3 账户简称（C 列）
```
=IF(D10="","",IFERROR(VLOOKUP(D10,账户列表!$A:$C,2,0),"未登记"))
```

### 3.4 累计充值（O 列）
```
=IF(C10="","",SUMIFS($F$10:$F$5900,$C$10:$C$5900,C10,$A$10:$A$5900,"<="&A10))
```

### 3.5 累计消耗（P 列，只含广告消耗不含手续费）
```
=IF(C10="","",SUMIFS($G$10:$G$5900,$C$10:$C$5900,C10,$A$10:$A$5900,"<="&A10))
```

### 3.6 存活账户余额（仪表盘 P4）
```
=SUMPRODUCT(($C$10:$C$5900<>"")*($E$10:$E$5900="正常")*
  (COUNTIFS($C$10:$C$5900,$C$10:$C$5900,$A$10:$A$5900,">"&$A$10:$A$5900)=0)*
  IFERROR($Q$10:$Q$5900*1,0))
```

### 3.7 按渠道汇总 A23（渠道清单，⚠️ 待修复，见七）

**修复后的正确公式**（直接取 B 列渠道值，不再用 REGEXEXTRACT 提取前缀）：
```
=IFERROR(UNIQUE(FILTER(
  VSTACK(投手1!$B$10:$B$5900,投手2!$B$10:$B$5900,...,投手10!$B$10:$B$5900),
  (IFERROR(VSTACK(投手1!$A$10:$A$5900,...)*1,0)>=$B$2)
  *(IFERROR(VSTACK(投手1!$A$10:$A$5900,...)*1,0)<=$D$2)
  *(VSTACK(投手1!$B$10:$B$5900,...)<>"")
)),"")
```

### 3.8 按渠道汇总 B~D 列（充值/广消/余额，以 row23 为例，公式本身正确）
```
=IF(A23="","",SUMPRODUCT(
  (IFERROR(vstack('投手1'!$B$10:$B$5900,...,'投手10'!$B$10:$B$5900),"")=A23)*1
  *(IFERROR(vstack('投手1'!$A$10:$A$5900,...)*1,0)>=$B$2)
  *(IFERROR(vstack('投手1'!$A$10:$A$5900,...)*1,0)<=$D$2)
  *IFERROR(vstack('投手1'!$F$10:$F$5900,...)*1,0)))
```

### 3.9 每日明细（QUERY，Row 46）
渠道直接取 B 列，`date '...'` 格式做日期比较：
```
=IFERROR(QUERY(FILTER({
  VSTACK(A列), VSTACK(B列渠道),
  IFERROR(VSTACK(F列充值)*1,0),
  IFERROR(VSTACK(G列广消)*1,0)},
  VSTACK(A列)<>""),
  "SELECT Col1,Col2,SUM(Col3),SUM(Col4)
   WHERE Col1 >= date '"&TEXT($B$2,"yyyy-mm-dd")&"'
   AND Col1 <= date '"&TEXT($D$2,"yyyy-mm-dd")&"'
   AND Col2 IS NOT NULL AND Col2 <> ''
   GROUP BY Col1,Col2 ORDER BY Col1 ASC,Col2 ASC
   LABEL Col1'',Col2'',SUM(Col3)'',SUM(Col4)''",0),"")
```

---

## 四、核心业务规则（不可随意更改）

1. **账户识别**：SUMIFS 匹配 **C 列（账户简称）**，简称来自账户列表 VLOOKUP，保证一致性
2. **唯一手填项**：投手只填 D 列账户编号，B/C 自动带出
3. **累计消耗**：只累加广告消耗（G），**不含手续费（H）**
4. **账户余额**：反映账户里还能花多少广告费，手续费不占余额
5. **提成公式**：开户成本**进括号**，投手只承担比例%的成本
6. **开户成本**：一次性，只在首次出现那天填，后续留空
7. **提成负数**：如实显示，不截断为 0
8. **存活余额**：被封账户不计入
9. **账户列表**：新账户必须先登记，否则投手 sheet 显示「未登记」
10. **Sheet 标签名不可改**：投手1~10 的 sheet 标签名是公式引用的硬编码，不能改；要改成真人名字只改 A1 标题文字

---

## 五、Google Sheets 函数依赖

以下函数**只在 Google Sheets 工作**，本地 xlsx 预览显示异常属正常：

| 函数 | 用途 |
|---|---|
| `VSTACK` | 跨 10 个投手 sheet 合并数据 |
| `UNIQUE` + `FILTER` | 动态列出渠道（A23） |
| `QUERY` | 日期×渠道交叉透视（每日明细） |
| `ARRAYFORMULA` | 批量计算 |

**本地预览现象**：按渠道汇总 A23 显示 fallback 值、每日明细显示 `#N/A` → **正常，导入 Google Sheets 后自动恢复**

---

## 六、导入 Google Sheets 规范

```
文件 → 导入 → 上传 → 创建新电子表格  ✅
                    替换电子表格      ❌ 会丢公式
```

---

## 七、⚠️ 待办：A23 渠道汇总公式修复（线上 Google Sheets）

**现象**：汇总 sheet 按渠道汇总区域（A23起）整行全部显示 `#REF!`

**根因**：A23 单元格目前残留的公式是旧版 REGEXEXTRACT 逻辑，且字符串拼接处出现截断错误（`'投手10'!$B$10"&":$B$5900"` 这种损坏片段），公式本身已损坏，不是 sheet 改名导致的问题。

**修复方法**：在 Google Sheets 中，直接选中 A23，删除原公式，粘贴以下新公式（取 B 列渠道值，不再提取前缀，按 B2/D2 时间范围过滤去重）：

```
=IFERROR(UNIQUE(FILTER(VSTACK(投手1!$B$10:$B$5900,投手2!$B$10:$B$5900,投手3!$B$10:$B$5900,投手4!$B$10:$B$5900,投手5!$B$10:$B$5900,投手6!$B$10:$B$5900,投手7!$B$10:$B$5900,投手8!$B$10:$B$5900,投手9!$B$10:$B$5900,投手10!$B$10:$B$5900),(IFERROR(VSTACK(投手1!$A$10:$A$5900,投手2!$A$10:$A$5900,投手3!$A$10:$A$5900,投手4!$A$10:$A$5900,投手5!$A$10:$A$5900,投手6!$A$10:$A$5900,投手7!$A$10:$A$5900,投手8!$A$10:$A$5900,投手9!$A$10:$A$5900,投手10!$A$10:$A$5900)*1,0)>=$B$2)*(IFERROR(VSTACK(投手1!$A$10:$A$5900,投手2!$A$10:$A$5900,投手3!$A$10:$A$5900,投手4!$A$10:$A$5900,投手5!$A$10:$A$5900,投手6!$A$10:$A$5900,投手7!$A$10:$A$5900,投手8!$A$10:$A$5900,投手9!$A$10:$A$5900,投手10!$A$10:$A$5900)*1,0)<=$D$2)*(VSTACK(投手1!$B$10:$B$5900,投手2!$B$10:$B$5900,投手3!$B$10:$B$5900,投手4!$B$10:$B$5900,投手5!$B$10:$B$5900,投手6!$B$10:$B$5900,投手7!$B$10:$B$5900,投手8!$B$10:$B$5900,投手9!$B$10:$B$5900,投手10!$B$10:$B$5900)<>""))),"")
```

粘贴后公式会自动溢出填充下方行（Google Sheets 数组溢出特性），不需要逐行填写。B~D 列公式本身没问题，不用改。

**修复后请验证**：
- A23 起应显示真实渠道名（Qdsk、HMB、LBYXR、Uid Reg 等），不再是 #REF!
- B/C/D 列对应数字应该正常显示金额而不是 #REF!

---

## 八、其他已知数据问题（待用户自行修正，非公式问题）

### 8.1 薯条（投手3）日期格式错误，导致提成列 #REF!

两行日期填错了格式：
- `2006-06-09`（年份写成了 2006，应为 2026）
- 第7行同样是 `2006-06-09`

这两行导致薯条 sheet 的累计列（O/P/Q）和提成列（T）从该行起全部变成 `#REF!`，连带合计行和「本期累计提成」也显示 `#REF!`。

**修复方法**：把这两行 A 列日期手动改为 `2026-06-09`。

### 8.2 部分行账户状态（E列）为空

以下行的 E 列（账户状态）没有选择「正常/封控」：
- 旺仔（投手2）：2026-06-09 两行 gosyj 账户、2026-06-06 LBYXR-A2557+8
- 薯条（投手3）：2026-06-10~13 多行 tdhnsm/XM/TDJMT 账户

**影响**：仪表盘「当前正常账户数」「当前封控账户数」统计会漏掉这些账户。

**修复方法**：补选 E 列下拉为「正常」或「封控」。

### 8.3 自检结果（使用说明 B34~B37）

| 检查项 | 当前差异 | 状态 | 说明 |
|---|---|---|---|
| 总充值 | ¥16,839.05 | ⚠ 失败 | 因 8.1/8.2 数据问题导致 |
| 实际消耗 | ¥6,745.88 | ⚠ 失败 | 同上 |
| 总提成 | #REF! | #REF! | 因薯条 #REF! 导致，修复 8.1 后会恢复 |
| 开户成本 | - | ✓ 通过 | 正常 |

修复 8.1、8.2 并确保 B2/D2 时间范围覆盖所有数据后，自检应全部转为 ✓ 通过。

---

## 九、加新投手操作流程

**10 人以内**：A1 标题改成真人姓名即可（sheet 标签名不变），汇总公式不变。

**超过 10 人**：
1. 复制「投手10」sheet → 改名标签为「投手11」
2. 汇总 sheet 里所有 `VSTACK(...,投手10!...)` 末尾加入投手11（约 10 处，包括第三节中所有公式）
3. 按投手汇总区域 row19（合计行）上方插入新行，填入投手11 的 SUMPRODUCT 公式
4. 合计行 SUM 范围扩展：`SUM(B9:B18)` → `SUM(B9:B19)`
5. 使用说明自检公式加入投手11，行号引用 5901

---

## 十、日常使用流程

1. **新开账户**：先在「账户列表」sheet 填入编号 / 简称 / 渠道（线上版还多了一列「日期」记录登记时间，可选填）
2. **每日填报**：投手 sheet 填 A(日期)、D(编号)、E(状态)、F(充值)、G(广消)、H(手续费)、J~M(开户当天填成本)、R(单价)、S(获客数)
3. **查看汇总**：汇总 sheet 直接修改 B2 / D2（手填，格式 yyyy-mm-dd），覆盖所有数据日期范围

---

## 十一、重要设计决策历史

| 决策点 | 选择 | 备注 |
|---|---|---|
| 账户名识别 | SUMIFS 匹配 C列(简称) | 解决换行符匹配失败的 bug |
| 账户简称来源 | VLOOKUP 从账户列表带出 | 解决拼写不一致导致累计数字静默出错 |
| 投手填写入口 | 只填账户编号（D列） | 简称/渠道自动带出，防止手填错误 |
| 渠道识别 | 账户列表直接维护渠道列 | B列直接=渠道值，A23 取UNIQUE即可，不再用REGEXEXTRACT |
| 多投手方案 | 每人一个 sheet | 维护成本较高，用户坚持 |
| 投手命名 | A1标题改真人名，sheet标签名不变 | 公式引用sheet标签名，标签名不能动 |
| 提成公式 | 成本进括号只扣比例% | 用户明确选择 A 方案 |
| 累计消耗口径 | 只含广告消耗，不含手续费 | 仪表盘「累计消耗」含手续费 |
| 数据行数 | 5900 行/投手 | 用户要求扩展 |
| 投手数量 | 10 人 | 用户要求从 5 扩展到 10 |
| QUERY 日期比较 | FILTER 去空行 + `date '...'` | 直接数字比较会因类型不匹配失效 |
| 汇总时间范围 | 手填固定日期 | 动态公式导致无法手动调整，改回手填 |
| 渠道汇总行数 | 20 行（A23:A42） | 原 10 行不够，扩展以支持最多 20 个渠道 |

---

## 十二、用户工作风格

> **核心原则：先验证，再动手（优先级最高）**
> - 面对不确定问题，先做最小实验验证，再写代码
> - 承认不确定：不确定就说「不确定」
> - 成本意识：瞎猜导致的返工 = 浪费用户的钱

---

## 十三、快速恢复说明

容器重置后脚本消失，如需重新修改文件：

1. 用户上传最新 xlsx 文件，或粘贴 Google Sheets 导出的内容
2. 告知 Claude「按照项目交接文档的规格检查/修改」
3. Claude 先读文件结构再动手，不乱猜公式

---

## 十四、本对话完成的工作

| 工作项 | 详情 |
|---|---|
| 检查线上 Google Sheets 数据 | 发现 A23 渠道汇总公式损坏（#REF!） |
| 定位根因 | A23 公式是旧版 REGEXEXTRACT 逻辑且字符串拼接处损坏，非 sheet 改名问题 |
| 提供修复公式 | 改为直接 VSTACK B列渠道值 + UNIQUE + FILTER，需用户手动粘贴到 A23 |
| 识别数据问题 | 薯条 sheet 两行日期年份错误（2006→应为2026）导致 #REF! 连锁 |
| 识别数据问题 | 多个账户状态(E列)为空，影响仪表盘账户数统计 |
| 确认 sheet 命名规范 | A1标题可改真人名，sheet标签名（投手1~10）不可改 |

---

*本文档由 Claude 自动整理，基于 v7 文件结构 + Google Sheets 线上实测数据，涵盖完整设计决策、技术细节与待办事项。*
