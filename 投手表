# 📋 项目交接文档 — 广告投手提成管理系统

> 本文档用于新对话窗口衔接，请将全文复制粘贴给新的 Claude 对话。
> 整理时间：2026-05-31（本对话完结版）

---

## 一、项目概览

### 交付物清单

| 文件名 | 说明 | 状态 |
|---|---|---|
| `投手提成_10投手版_v5.xlsx` | 主文件，最终版，12 个 sheet | ✅ 已完成 |
| `日常收支记录表.xlsx` | 日常收支记录，独立文件 | ✅ Google Drive 存档 |
| Google Sheets（线上） | 导入自 v5 xlsx，功能已验证 | ✅ 已确认正常 |

### 业务背景

管理多名「投手」（广告投放人员）的每日广告投放数据与提成结算。

```
当日提成 = 提成比例 × (有效获客数 × 单价 − 实际总消耗 − 当日开户成本合计)
```

---

## 二、当前文件规格（最终版）

### 2.1 文件结构（12 个 sheet）

```
汇总 → 投手1 → 投手2 → ... → 投手10 → 使用说明
```

### 2.2 投手 sheet 规格（投手1~10，结构完全一致）

**列结构（A~S，19列）**

| 列 | 名称 | 类型 |
|---|---|---|
| A | 日期 | 手填 |
| B | 账户简称 | 手填，参与所有计算 |
| C | 账户编号 | 手填，仅显示 |
| D | 账户状态 | 下拉：正常/封控 |
| E | 当日充值 | 手填 |
| F | 广告消耗 | 手填 |
| G | 手续费 | 手填 |
| H | 实际总消耗 | 公式：F+G |
| I~L | 账户/个号/BM/IP成本 | 手填，仅首次开户当天填 |
| M | 成本合计 | 公式：I+J+K+L |
| N | 累计充值 | SUMIFS 按账户简称+日期 |
| O | 累计消耗 | SUMIFS 只算广告消耗（F列） |
| P | 账户余额 | N-O |
| Q | 单价 | 手填 |
| R | 有效获客数 | 手填 |
| S | 当日提成 | 公式：$A$4×(R×Q−H−M) |

**数据区**：第 10~5900 行（5891 行）  
**合计行**：第 5901 行  
**冻结**：E10（前 9 行 + 前 4 列）  
**自动筛选**：A9:S5900

### 2.3 仪表盘（每个投手 sheet，Row 3~6，8 项）

| Row | 内容 |
|---|---|
| 3/4 | 提成比例（A4，浅黄底，默认30%） \| 正常账户数 \| 封控账户数 \| 存活账户余额 |
| 5/6 | 累计总充值 \| 累计开户成本 \| 累计消耗 \| 累计提成 |

### 2.4 汇总 sheet 结构（最终行号）

```
Row 1      主标题
Row 2      时间范围 B2/D2（浅黄底，可改，全表联动）
Row 3      空行
Row 4~5    仪表盘 8 项
Row 6      空行
Row 7      — 按投手汇总 — 标题
Row 8      列标题（灰底）
Row 9~18   投手1~10 各行（SUMPRODUCT，按日期范围统计）
Row 19     合计行 SUM(B9:B18)
Row 20     空行
Row 21     — 按渠道汇总 — 标题（italic）
Row 22     列标题（灰底）：渠道 | 充值 | 广告消耗 | 总正常账户余额
Row 23~32  渠道数据（10行，A23=UNIQUE公式，B~D=各行引用自己A列）
Row 33     空行
Row 34     — 每日明细 — 标题（italic）
Row 35     列标题（灰底）：日期 | 渠道 | 充值 | 广告消耗
Row 36     QUERY公式（每日明细数据）
```

---

## 三、核心公式说明

### 3.1 提成公式（S 列）
```
=IF(OR(B10="",AND(R10="",Q10="",H10="",M10="")),"",
  $A$4*(N(R10)*N(Q10)-N(H10)-N(M10)))
```

### 3.2 累计充值（N 列）
```
=IF(B10="","",SUMIFS($E$10:$E$5900,$B$10:$B$5900,B10,$A$10:$A$5900,"<="&A10))
```

### 3.3 累计消耗（O 列，只含广告消耗不含手续费）
```
=IF(B10="","",SUMIFS($F$10:$F$5900,$B$10:$B$5900,B10,$A$10:$A$5900,"<="&A10))
```

### 3.4 存活账户余额（仪表盘 P4）
```
=SUMPRODUCT(($B$10:$B$5900<>"")*($D$10:$D$5900="正常")*
  (COUNTIFS($B$10:$B$5900,$B$10:$B$5900,$A$10:$A$5900,">"&$A$10:$A$5900)=0)*
  IFERROR($P$10:$P$5900*1,0))
```

### 3.5 按渠道汇总 B 列（充值，每行引用自己的 Aref）
以 row23 为例（其余行依此类推，Aref 改为对应行号）：
```
=IF(A23="","",SUMPRODUCT(
  IFERROR((LEFT(vstack('投手1'!$B$10:$B$5900,...,'投手10'!$B$10:$B$5900),LEN(A23))=A23)*1,0)
  *(IFERROR(vstack('投手1'!$A$10:$A$5900,...)*1,0)>=$B$2)
  *(IFERROR(vstack('投手1'!$A$10:$A$5900,...)*1,0)<=$D$2)
  *IFERROR(vstack('投手1'!$E$10:$E$5900,...)*1,0)))
```

### 3.6 每日明细（QUERY，Row 36）
关键：Col1 保持日期类型（不乘*1），用 `date '...'` 格式比较：
```
=IFERROR(QUERY(FILTER({
  VSTACK('投手1'!$A$10:$A$5900,...,'投手10'!$A$10:$A$5900),
  ARRAYFORMULA(IFERROR(REGEXEXTRACT(VSTACK(B列),"^[A-Za-z]+"),"")),
  IFERROR(VSTACK(E列)*1,0),
  IFERROR(VSTACK(F列)*1,0)},
  VSTACK(A列)<>""),
  "SELECT Col1,Col2,SUM(Col3),SUM(Col4)
   WHERE Col1 >= date '"&TEXT($B$2,"yyyy-mm-dd")&"'
   AND Col1 <= date '"&TEXT($D$2,"yyyy-mm-dd")&"'
   AND Col2 IS NOT NULL AND Col2 <> ''
   GROUP BY Col1,Col2 ORDER BY Col1 ASC,Col2 ASC
   LABEL Col1'',Col2'',SUM(Col3)'',SUM(Col4)''",0),"")
```

---

## 四、核心业务规则（不可随意更改）

1. **账户名识别**：SUMIFS 匹配用 **B 列（账户简称）**，不用 C 列
2. **累计消耗**：只累加广告消耗（F），**不含手续费（G）**
3. **账户余额**：反映账户里还能花多少广告费，手续费不占余额
4. **提成公式**：开户成本**进括号**，投手只承担 30% 的成本
5. **开户成本**：一次性，只在首次出现那天填，后续留空
6. **提成负数**：如实显示，不截断为 0
7. **存活余额**：被封账户不计入

---

## 五、Google Sheets 函数依赖

以下函数**只在 Google Sheets 工作**，本地 xlsx 预览显示异常属正常：

| 函数 | 用途 |
|---|---|
| `VSTACK` | 跨 10 个投手 sheet 合并数据 |
| `REGEXEXTRACT` | 从账户简称提取渠道前缀 |
| `UNIQUE` + `FILTER` | 动态列出渠道 |
| `QUERY` | 日期×渠道交叉透视（每日明细） |
| `ARRAYFORMULA` | 批量提取渠道 |

**本地预览现象**：汇总仪表盘部分数字显示 0、每日明细显示 `#N/A` → **正常，导入 Google Sheets 后自动恢复**

---

## 六、导入 Google Sheets 规范

```
文件 → 导入 → 上传 → 创建新电子表格  ✅
                    替换电子表格      ❌ 会丢公式
```

---

## 七、加新投手操作流程

**10 人以内**：把对应 sheet 的 A1 标题改成真人姓名即可，汇总公式不变。

**超过 10 人**：
1. 复制「投手10」sheet → 改名「投手11」
2. 汇总 sheet 里所有 `VSTACK(...,'投手10'!...)` 改成 `VSTACK(...,'投手11'!...)`（约 10 处）
3. 按投手汇总区域 row 19（目前合计行）上方插入新行，填入投手11 的 SUMPRODUCT 公式
4. 合计行 SUM 范围扩展：`SUM(B9:B18)` → `SUM(B9:B19)`
5. 使用说明自检公式加入投手11，行号引用 5901

---

## 八、示例数据（投手1 内置，其余空白）

| 日期 | 账户简称 | 状态 | 充值 | 广告消耗 | 手续费 | 开户成本 | 单价 | 获客 |
|---|---|---|---|---|---|---|---|---|
| 2026-05-01 | LBTG-A2062 | 正常 | 5000 | 1200 | 60 | 460 | 80 | 18 |
| 2026-05-01 | LBTG-A2063 | 正常 | 3000 | 800 | 40 | 460 | 90 | 11 |
| 2026-05-02 | LBTG-A2062 | 正常 | — | 1500 | 75 | — | 80 | 22 |
| 2026-05-02 | LBTG-A2063 | 正常 | — | 950 | 48 | — | 90 | 12 |
| 2026-05-03 | LBTG-A2062 | **封控** | — | 600 | 30 | — | 80 | 9 |
| 2026-05-03 | LBTG-A2063 | 正常 | — | 1100 | 55 | — | 90 | 14 |
| 2026-05-03 | LBTG-A2064 | 正常 | 4000 | 700 | 35 | 460 | 80 | 11 |

**已校验关键数字**：
- 累计提成 = **¥−171.90**（实际数据，含投手2~10真实数据后会变）
- 存活账户余额 = **¥43,400**（含投手2~10真实数据）
- 投手1 示例：提成 ¥−132.90，存活余额 ¥3,450，正常账户 2，封控 1

---

## 九、本对话完成的工作

本对话在上一版基础上完成了以下改动：

| 工作项 | 详情 |
|---|---|
| 每日明细 QUERY 修复 | FILTER 去空行 + `date '...'` 日期比较，解决空白问题 |
| 仪表盘公式简化 | VSTACK → SUM(按投手汇总)，xlsx 本地也能正确显示 |
| 数据行扩展至5900行 | 全表 `$59` → `$5900`，合计行移至 row5901 |
| 投手数量 5→10 | 新增投手6~10 sheet，汇总所有公式扩展 |
| 汇总行下移修复 | 行样式、格式、合并单元格等遗留问题全部修复 |
| 按渠道汇总修复 | B/C/D 列公式重建：各行引用自己 A 列，投手1~10，`$5900` |

---

## 十、日常收支记录表规格（独立文件）

7 列：日期 | 收入明细 | 收入金额 | 支出明细 | 支出金额 | **当日利润** | **累计利润**

- 当日利润 = `IFERROR(N(收入金额)-N(支出金额),0)`
- 累计利润逐行累加
- 合计行汇总总收入、总支出、净利润
- 预留 30 行，首行冻结，筛选器已开启

---

## 十一、重要设计决策历史

| 决策点 | 选择 | 备注 |
|---|---|---|
| 账户名拆列 | ✅ 拆简称+编号 | 解决 SUMIFS 因换行符匹配失败的 bug |
| 渠道识别 | 公式自动提取 REGEXEXTRACT | 风险：账户名无字母前缀时落空 |
| 多投手方案 | 每人一个 sheet | 维护成本较高，用户坚持 |
| 提成公式 | 成本进括号只扣 30% | 用户明确选择 A 方案 |
| 累计消耗口径 | 只含广告消耗，不含手续费 | 仪表盘「累计消耗」保持含手续费 |
| 数据行数 | 5900 行/投手 | 用户要求扩展 |
| 投手数量 | 10 人 | 用户要求从 5 扩展到 10 |
| QUERY 日期比较 | FILTER去空行 + `date '...'` | 直接数字比较会因类型不匹配失效 |

---

## 十二、用户工作风格

> **核心原则：先验证，再动手（优先级最高）**
> - 面对不确定问题，先做最小实验验证，再写代码
> - 承认不确定：不确定就说「不确定」
> - 成本意识：瞎猜导致的返工 = 浪费用户的钱

---

## 十三、快速恢复说明

容器重置后脚本消失，如需重新修改文件：

1. 用户上传最新 xlsx 文件
2. 告知 Claude「按照项目交接文档的规格检查/修改」
3. Claude 先读文件结构再动手，不乱猜公式

---

*本文档由 Claude 自动整理，涵盖完整设计决策与技术细节。*
